<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Quora</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap" rel="stylesheet">
  <style>
    body {
      margin: 0;
      background-color: #f1f2f3;
      font-family: 'Inter', sans-serif;
    }

    header {
      background-color: white;
      padding: 1rem 2rem;
      border-bottom: 1px solid #ddd;
      font-size: 1.5rem;
      font-weight: 600;
      color: #b92b27;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .create-btn {
      background-color: #b92b27;
      color: white;
      border: none;
      padding: 0.5rem 1rem;
      font-size: 0.95rem;
      border-radius: 6px;
      cursor: pointer;
      transition: background-color 0.3s ease;
    }

    .create-btn:hover {
      background-color: #a82322;
    }

    .container {
      max-width: 700px;
      margin: 2rem auto;
      padding: 0 1rem;
    }

    .post {
      background-color: white;
      border: 1px solid #e2e2e2;
      border-radius: 8px;
      padding: 1rem 1.5rem;
      margin-bottom: 1.2rem;
      box-shadow: 0 2px 5px rgba(0,0,0,0.05);
    }

    .post-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 0.5rem;
    }

    .username {
      font-weight: 600;
      color: #333;
      text-decoration: none;
    }

    .edit-btn, .delete-btn {
      background-color: transparent;
      border: 1px solid #b92b27;
      color: #b92b27;
      padding: 0.3rem 0.8rem;
      font-size: 0.85rem;
      border-radius: 4px;
      cursor: pointer;
      text-decoration: none;
      transition: all 0.3s ease;
      margin-left: 0.4rem;
    }

    .edit-btn:hover {
      background-color: #b92b27;
      color: white;
    }

    .delete-btn:hover {
      background-color: #e53935;
      border-color: #e53935;
      color: white;
    }

    .content {
      font-size: 1rem;
      color: #444;
    }

    form {
      display: inline;
    }
  </style>
</head>
<body>

  <header>
    Quora
    <a href="/posts/new">
      <button class="create-btn">Create New Post</button>
    </a>
  </header>

  <div class="container">
    <% for (let i = 0; i < posts.length; i++) { %>
      <div class="post">
        <div class="post-header">
          <a href="/posts/<%= posts[i].id %>" class="username">@<%= posts[i].username %></a>
          <div>
            <a href="/posts/<%= posts[i].id %>/edit" class="edit-btn">Edit</a>
            <form action="/posts/<%= posts[i].id %>?_method=DELETE" method="POST">
              <button class="delete-btn" type="submit" onsubmit="return confirm('Are you sure you want to delete this post?');">Delete</button>
            </form>
          </div>
        </div>
        <div class="content"><%= posts[i].content %></div>
      </div>
    <% } %>
  </div>

</body>
</html>
